{{#each users as |user| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{{user.images}}} {{user.display_name}}</h2>
  </div>
  <div class="col-md-6">
    <h3>{{user.playlist.name}}</h3>
    <h3>{{user.playlist.name}}</h3>
    <h3>{{user.playlist.name}}</h3>
  </div>
</div>
{{/each}}

